package core

import (
//	"fmt"
//	"testing"
)

/*
func TestIsTagged(t *testing.T) {

	e := tag(core.Sym("mac"), 
	         core.Sym("foo"))

	got := isTagged(e)
	want := core.T
	
	if got.NotEqual(want) {
		t.Errorf("isTagged failed - got:%v, want:%v", got, want)
	}

}

func TestTag(t *testing.T) {

	mac := core.Sym("mac")
	exp := core.Sym("foo")

	got := fmt.Sprintf("%v", tag(mac, exp))
	want := "((quote tagged) mac foo)"
	
	if got != want {
		t.Errorf("tag failed - got:%v, want:%v", got, want)
	}	

}

func TestType0(t *testing.T) {

	mac := core.Sym("mac")
	exp := core.Sym("foo")
	
	got := type0(tag(mac, exp))
	want := mac
	
	if got.NotEqual(want) {
		t.Errorf("type0 failed - got:%v, want:%v", got, want)
	}
	
}

func TestRep(t *testing.T) {

	mac := core.Sym("mac")
	exp := core.Sym("foo")
	
	got := rep(tag(mac, exp))
	want := exp
	
	if got.NotEqual(want) {
		t.Errorf("rep failed - got:%v, want:%v", got, want)
	}
	
}
*/
